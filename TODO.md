# TODO

## Done

- [x] Scaffold folders, packaging, CI, and documentation skeleton.
- [x] First code milestone: `project_root`, `load_raw_csv`, and unit tests.

## Next

- [ ] Lock dataset choice, label definition, and horizon; document in `docs/architecture.md`.
- [ ] Add schema validation for raw ingests (fail fast on drift).
- [ ] Baseline model + reproducible training entrypoint (CLI or script under `src/`).

## Hygiene

- [ ] Add `pre-commit install` to your local clone if not already installed.
